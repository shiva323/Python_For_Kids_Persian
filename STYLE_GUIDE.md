# راهنمای طراحی جلسات آینده

## الگوی تدریس
1. Hero کوتاه: عنوان + ۳ تا ۵ هدف قابل سنجش
2. هر کارت فقط یک مفهوم
3. تصویر فقط وقتی مفهوم را روشن‌تر می‌کند
4. Predict: قبل از اجرا، خروجی حدس زده شود
5. Run: کد اجرا شود
6. Modify: یک چیز در کد تغییر کند
7. Debug: یک خطای واقعی پیدا شود
8. Build: مینی‌پروژه
9. Stretch: چالش اضافه

## قواعد بصری
- فارسی RTL
- کد LTR
- پاراگراف بلند ممنوع
- بین هر بخش خط جداکننده
- تصاویر داخل `images/` و با مسیر نسبی `./images/...`
- یک رنگ اصلی برای هر جلسه
- ایموجی فقط برای نشانه‌گذاری، نه شلوغ‌کاری


## استاندارد جدید جلسات 09+
- هر جلسه حداقل دو تصویر مفهومی/خلاقانه انگلیسی داشته باشد.
- تصاویر باید داخل خود Notebook به صورت PNG Attachment ذخیره شوند.
- یک نسخه فایل نیز در `images/` باقی بماند.
- هر جلسه حداقل یک Predict، یک Debug و یک Project داشته باشد.
- مفاهیم جدید باید پروژه‌های قبلی را دوباره استفاده کنند تا یادگیری پیوسته باشد.
