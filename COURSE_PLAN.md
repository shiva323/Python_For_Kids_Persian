# برنامه دوره Python for Kids

1. الگوریتم، شبه‌کد و فلوچارت
2. `print`، `input` و متغیر
3. کامنت، `f-string`، انواع داده، حافظه، تبدیل نوع و خطا
4. عملگرهای ریاضی، ترتیب عملیات و `math`
5. پروژه ماشین‌حساب چندکاره
6. String: Concatenation، Indexing، Negative Indexing، Slicing، Step، Repeat، Escape Characters، Immutability
7. String Methods: `upper()`، `lower()`، `title()`، `capitalize()`، `strip()`، `replace()`، `len()`، `count()`، `find()`، `startswith()`، `endswith()`، `isdigit()`، `isalpha()`
8. Comparison + Logical Operators مقدماتی
9. `if / elif / else`، شرط‌های ترکیبی، Nested Conditions، Membership و Truthiness مقدماتی
10. Loop Concepts: `for`، `range`، iteration، counter، accumulator، min/max، average، `enumerate`، `break`، `continue`، `while`، nested loop
11. Lists: indexing، slicing، traversal، mutability، add/remove methods، sorting، alias/copy، nested lists
12. دیکشنری‌ها
13. توابع
14. `random` و بازی
15. پروژه Quiz
16. فایل‌ها و ذخیره‌سازی
17. پروژه نهایی
18. ارائه پروژه و مسیر بعدی
